// Описание работы кода

int calc(int* arr, int size)
{
    int ans = 0;
    for (int i = 1; i < size; i++)
    {
        ans += arr[i] - arr[i - 1];
    }
    return ans;
}




//Код представленной функции `calc` на языке C++ выполняет следующие операции :

//1. * *Входные параметры * *: Функция принимает два аргумента — указатель на массив целых чисел `arr` и целое число `size`, представляющее размер этого массива.

//2. * *Инициализация результата * *: Переменная `ans` инициализируется значением 0. Она будет использоваться для хранения суммы разностей между соседними элементами массива.

3//. * *Цикл по элементам массива * *: Используется цикл for для итерации по массиву, начиная со второго элемента(индекс 1) и заканчивая последним(индекс `size - 1`).
   // - На каждой итерации вычисляется разность между текущим элементом `arr[i]` и предыдущим элементом `arr[i - 1]`.
  //  - Эта разность добавляется к переменной `ans`.

   // 4. * *Возврат результата * *: После завершения цикла осуществляется возврат значения переменной `ans`, что представляет собой сумму всех разностей между соседними элементами массива.

   // ### Асимптотика

  //  #### Временная сложность
  //  - Временная сложность алгоритма составляет \(O(n)\), где \(n\) — это размер массива(`size`).
   //     - Это связано с тем, что в цикле производится ровно \(n - 1\) операций(одна операция для каждой пары соседних элементов в массиве).
   //     - Каждый шаг выполняется за постоянное время, следовательно, общее время работы функции линейно зависит от размера входных данных.

     //   #### Пространственная сложность
     //   - Пространственная сложность алгоритма составляет \(O(1)\).
     //   - Функция использует фиксированное количество дополнительных переменных(в данном случае только переменную `ans`), которые не зависят от размера входного массива.
      //      - Таким образом, память, занимаемая дополнительными переменными, остаётся постоянной, независимо от размера массива `arr`.

        //    ### Аргументация
        //    - **Временная сложность \(O(n)\) * *: Каждый элемент массива обрабатывается ровно один раз, итерация по массиву требует линейного времени, поскольку необходимо выполнить операции для каждого элемента с 1 до \(n - 1\).

        //    - **Пространственная сложность \(O(1)\) * *: Эта функция не создает новые массивы или структуры данных, а просто использует одну переменную для хранения результата.Поэтому, независимо от входного размера, память не увеличивается.